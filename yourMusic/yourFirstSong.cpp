// remember to add #include "../winMusLib/winmuslib.h" 
// at the top of your music files for them to work!

#include "../src/winmuslib.h"

int main()
{
  /* Write your notes here! example: 
   *
   * c3(300);
   *
   * remember the duration (300) is in milliseconds.
   *
   * Also remember that there is no overlapping of beeps, so melodies only. (unless you have multiple Windows machines haha)
   */
   
  return 0;
}
